import type { VisualEffect } from "@/lib/cellEffectMapping";import { truthDescription } from "@/lib/cellEffectMapping";import { Badge } from "@/components/ui/badge";import { Activity,BookOpen,Dna,GitBranch,ShieldCheck } from "lucide-react";
export function CellInspector({effect}:{effect:VisualEffect}){return <div className="cell-inspector"><div className="inspector-head"><div><div className="rail-label">WHY AM I SEEING THIS?</div><h2>{effect.process}</h2></div><Badge variant="outline">{effect.truth}</Badge></div><div className="inspector-section"><Activity/><div><b>Visual state</b><p>{effect.direction} · {Math.round(effect.intensity*100)}% mapped intensity</p></div></div><div className="inspector-section"><Dna/><div><b>Mapped genes</b><div className="gene-pills">{effect.genes.map(g=><span key={g}>{g}</span>)}</div></div></div><div className="inspector-section"><GitBranch/><div><b>Pathway</b><p>{effect.pathway??"No direct pathway match in this slice"}</p></div></div><div className="inspector-section"><ShieldCheck/><div><b>Truth state</b><p>{truthDescription[effect.truth]}</p></div></div><div className="inspector-section"><BookOpen/><div><b>Provenance</b><p>{effect.why}</p><code>{effect.evidenceIds.join(" · ")}</code></div></div></div>}